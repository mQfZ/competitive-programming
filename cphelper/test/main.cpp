#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int x; cin >> x;
    cout << x * 2 << '\n';
    cout << 5 << '\n';
    if (x == 12) {
        cout << 2 << '\n';
    }
    vector<int> a(x);
    if (a[x] == 0) {
        
    }
}
